### Crear una nueva branch y pushearla al origin

```bash
$ git checkout -b {nombre}
$ git push --set-upstream origin {nombre}
```

### Chequear en que branch estas

```bash
$ git branch
```

### Listar las branches existentes

```bash
$ git branch -a
```